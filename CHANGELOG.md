# Lumir 版本更新日志

## 1.3.3

- 适配 Lumic 背景大图、原图轮播与阻尼手势：关闭 WebView 原生边缘回弹，避免和网页动画叠加，不拦截网页触控操作。
- 跟随网页日夜主题同步系统栏、系统栏图标和 WebView 底色，禁止 WebView 自动改变网页配色。
- 使用 AndroidX 返回分发处理导航键与系统返回手势，保留网页自身的弹窗、背景大图及动态页面返回动画。
- 兼容新版与旧版会话、退出登录接口；原图及随机图库 API 仍由网页正常请求，不纳入客户端预览缓存。
- 布局、搜索栏、切图动画继续由 Lumic 服务端统一提供，客户端不注入重复 CSS。

## 1.3.2

- 适配 Lumic 动态页新版布局：使用网页响应式视口和正常文字排版，保留系统字体缩放设置。
- 隐藏 WebView 原生滚动条，与动态页按需加载和无滚动条展示保持一致。
- 移除旧版原生下拉刷新触摸监听，刷新交由页面按钮处理，避免与图片和页面切换手势重复响应。
- 作者和标签背景、卡片时间、展开按钮和悬浮样式继续由服务端统一提供，客户端不注入重复 CSS。

## 1.3.1

- 移除 Android WebView 的强制独立硬件层设置，恢复系统默认网页合成路径。
- 修复 Lumic 手机模式下三个悬浮按钮的磨砂、伪元素和图层效果与手机浏览器显示不一致的问题。
- 悬浮按钮继续完全使用 Lumic 服务端提供的 DOM、CSS 和交互逻辑，Android 客户端不注入或覆盖按钮样式。

## 1.3.0

- 将登录页的“关于”和图片缓存设置入口分开，设置入口改为右下角独立图标。
- 图片缓存设置改为独立页面，支持左上角返回按钮、系统导航返回键和全面屏返回手势。
- 图片缓存上限默认 256 MB，可通过滑动条在 128 MB 至 5120 MB 范围内按 128 MB 调整。
- 新增当前缓存空间统计和“立即清理”操作，缓存超过上限时继续按最早写入顺序自动清理。
- 加深“关于”弹窗标题颜色，提高浅色界面下的文字可读性。
- 移除 Android 客户端对 Lumic 三个悬浮按钮的样式注入，由服务端 DOM、CSS 和交互逻辑完整控制显示。

## 1.2.1

- 优化检查更新后的新版本下载窗口，改为与当前软件界面一致的圆角风格。
- 重新绘制登录页图片缓存设置入口，使其与“关于 Lumir · 版本与更新”按钮尺寸、圆角和配色保持一致。
- 优化图片缓存设置窗口，统一圆角、按钮颜色和中文界面样式。
- 保持 Lumic 服务端动态页悬浮按钮及其显示样式不变，不在 Android 客户端中额外覆盖或重绘。

## 1.2.0

- 新增作者头像和动态图片预览图的本地缓存，默认容量为 256 MB。
- 登录页右下角新增图片缓存设置入口，可将容量调整为 32 至 1024 MB。
- 缓存超过上限时按最早写入顺序自动清理，并支持立即清空缓存。
- 适配 Lumic 当前 WebView 动态页，包括移动端导航、悬浮按钮和新的预览图片路径。

## 1.1.0

- 登录成功后默认进入全部动态页。
- 在默认页首次按返回键时提示“再按一次退出程序”，2 秒内再次按返回键退出应用。
- GitHub Release 自动附带当前版本的更新日志。
